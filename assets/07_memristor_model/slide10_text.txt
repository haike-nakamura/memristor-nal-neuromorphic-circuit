2.近期问题：忆阻器模型因此，结合文献以及仿真中的实际问题，我现将忆阻模型更改如下：Gx 0 x value={ Ioff/(I(Emem)-I0)*uv*stp(V(Plus,Minus)-Vt1)*Ron/D^2*f_inc(V(x),p) + I(Emem)*uv*stp(Vt2-V(Plus,Minus))*Ron/D^2*f_dec(V(x),p)}Cx x 0 1 IC={(Roff-Rinit)/(Roff-Ron)}Raux x 0 1TEmem Plus aux value={-I(Emem)*V(x)*(Roff-Ron)}Roff aux Minus {Roff}Eflux flux 0 value={SDT(V(Plus,Minus))}Echarge charge 0 value={SDT(I(Emem))}.func f_inc(x,p)={1-x^(2*p)*stp(x)+stp(-x)}.func f_dec(x,p)={1-(1-x)^(2*p)*stp(-x+1)+stp(x-1)}忆阻器代码（有省略）第一，将阻值上升和下降函数分别定义施加正电压使用f_inc，施加负电压用f_dec。确保更符合原文定义，并且保证忆阻器功能正常实现这一步确保了忆阻器可以触碰Ron/Roff而不被锁死阻值。第二，阶跃函数框限窗函数范围，并设置x超限后解决情况在保证忆阻器基本功能维持不变的基础下，增加对意外情况的处理能力。