2.近期问题：忆阻器模型在参考文献中，将忆阻器窗函数定义如下：其中忆阻阻值在Ron和Roff之间变动的相对位置，在代码中对应x。可以看到其2p次方内的常数项根据电流方向决定，即施加正负电压场景下，对应窗函数不同，也就是说，增加阻值和减少阻值对应的窗函数不同。 现实中的忆阻器件是连续变化的，窗函数对变化率的设置决定其永远不可能超出Ron或者Roff然而，在电路仿真中，由于步长设置和仿真数量级原因，实际上可能导致x滑出（0，1）的预设区间。此时不应出现的窗函数起到反向扩大偏差的作用，最终导致其完全不按照忆阻器件特性工作。